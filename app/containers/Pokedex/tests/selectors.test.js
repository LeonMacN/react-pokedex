// import { fromJS } from 'immutable';
// import { makeSelectPokedexDomain } from '../selectors';

// const selector = makeSelectPokedexDomain();

describe('makeSelectPokedexDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
